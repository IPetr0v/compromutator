#pragma once

struct InterceptorDiff_
{
    //std::vector<RuleInfo> rules_to_add;
    //std::vector<RuleInfo> rules_to_delete;
};

class InterceptorManager
{
public:
    /*InterceptorDiff_ popInterceptorDiff() {
        return std::move(latest_interceptor_diff_);
    }

    void addRule(RulePtr rule) {}
    void deleteRule(RulePtr rule) {}*/

private:
    //InterceptorDiff_ latest_interceptor_diff_;
};
